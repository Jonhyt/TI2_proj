@model IEnumerable<TI2_proj.Models.Bandas>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Banda.Img)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Membro.Img)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Dono)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Banda.Img)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Membro.Img)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Dono)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.BandaID }) |
            @Html.ActionLink("Details", "Details", new { id=item.BandaID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.BandaID })
        </td>
    </tr>
}

</table>
